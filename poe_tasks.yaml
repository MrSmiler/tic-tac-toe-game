
tasks:
    check-lint:
        help: "check code lint"
        shell: "ruff check"

    fix-lint:
        help: "fix code lint"
        shell: "ruff format"

    run-tests:
        help: "run unit tests"
        shell: "pytest"
    
    static-check:    
        help: "check type annotations"
        shell: "mypy --strict tic_tac_toe_game"

